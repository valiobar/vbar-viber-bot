/**
 * Carousel Domain Types
 *
 * Type definitions for the Carousel domain
 *
 * Re-exports from @vbar/shared for consistency across services.
 */

export type {
  CarouselDTO,
  CarouselCardDTO,
  CarouselCtaDTO,
  CarouselCardMode,
  CarouselCtaActionType,
  ButtonDTO,
  ActionType,
  OpenURLType,
  TextSize,
  TextVAlign,
  TextHAlign,
} from "@vbar/shared";
