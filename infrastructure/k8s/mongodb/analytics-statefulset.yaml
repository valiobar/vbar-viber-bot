apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mongodb-analytics
  namespace: vbar-viber-bot
  labels:
    app: mongodb-analytics
    component: database
    service: analytics-db
spec:
  serviceName: mongodb-analytics
  replicas: 1
  selector:
    matchLabels:
      app: mongodb-analytics
  template:
    metadata:
      labels:
        app: mongodb-analytics
        component: database
        service: analytics-db
    spec:
      containers:
      - name: mongodb
        image: mongo:7
        ports:
        - name: mongodb
          containerPort: 27017
          protocol: TCP
        env:
        - name: MONGO_INITDB_DATABASE
          value: "analytics"
        volumeMounts:
        - name: mongodb-data
          mountPath: /data/db
        resources:
          requests:
            memory: "1Gi"
            cpu: "1000m"
          limits:
            memory: "2Gi"
            cpu: "2000m"
        livenessProbe:
          exec:
            command:
            - mongosh
            - --eval
            - db.adminCommand('ping')
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          exec:
            command:
            - mongosh
            - --eval
            - db.adminCommand('ping')
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
  volumeClaimTemplates:
  - metadata:
      name: mongodb-data
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 100Gi








